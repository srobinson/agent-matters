# manifests fixtures

Profile and capability manifest fixtures live here as the manifest schemas
come online under ALP-1911.

Current fixture groups:

* `profiles/`: profile manifests covering valid schemas and parse failures.
* `capabilities/`: capability manifests covering valid schemas and parse
  failures.
